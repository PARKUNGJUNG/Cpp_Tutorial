//헤더 파일(#include) : 프로그램에 필요한 도구(라이브러리)를 가져옵니다.

//반환타입 함수이름(매개변수) {
//    // 실행할 코드
//}

//반환타입: 함수가 작업을 마친 후 돌려줄 결과의 타입(예 : 숫자, 문자 등).결과가 없으면 void를 사용.
//함수이름 : 함수를 부를 때 사용할 이름(예 : worker).
//매개변수 : 함수가 작업할 때 필요한 입력값(없을 수도 있음).
//중괄호{} : 함수가 실제로 하는 일을 작성하는 부분.